import * as reports from "../services/report.service.js";import {publishEvent} from "../messaging/publisher.js";import {priorityUpdateSchema,reportFiltersSchema,reportIdSchema,reviewReportSchema,statusUpdateSchema} from "../validators/report.validator.js";import {enrichReports} from "../services/location-client.service.js";
async function publishChanges(before,after){if(before.status!==after.status)await publishEvent("report.status.changed",{reportId:after.id,citizenId:after.citizenId,previousStatus:before.status,newStatus:after.status,changedAt:after.updatedAt});if(before.priority!==after.priority)await publishEvent("report.priority.changed",{reportId:after.id,citizenId:after.citizenId,previousPriority:before.priority,newPriority:after.priority,changedAt:after.updatedAt});}
export async function list(request,response){const filters=reportFiltersSchema.parse(request.query);response.json({success:true,reports:await enrichReports(await reports.getAllReports(filters))});}export async function detail(request,response){const {id}=reportIdSchema.parse(request.params);const [report]=await enrichReports([await reports.getReportById(id)]);response.json({success:true,report});}export async function review(request,response){const {id}=reportIdSchema.parse(request.params);const input=reviewReportSchema.parse(request.body);const before=await reports.getReportById(id);const report=await reports.updateReport(id,input);await publishChanges(before,report);response.json({success:true,message:"Report review updated",report});}export async function status(request,response){const {id}=reportIdSchema.parse(request.params);const input=statusUpdateSchema.parse(request.body);const before=await reports.getReportById(id);const report=await reports.updateReport(id,input);await publishChanges(before,report);response.json({success:true,message:"Report status updated",report});}export async function priority(request,response){const {id}=reportIdSchema.parse(request.params);const input=priorityUpdateSchema.parse(request.body);const before=await reports.getReportById(id);const report=await reports.updateReport(id,input);await publishChanges(before,report);response.json({success:true,message:"Report priority updated",report});}export async function summary(_request,response){response.json({success:true,summary:await reports.getAdminSummary()});}
