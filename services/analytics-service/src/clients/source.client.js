import {AppError} from "../utils/app-error.js";export async function sourceRequest(baseUrl,path,name){if(!baseUrl)throw new AppError(`${name} analytics configuration unavailable`,503);const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),4000);try{const response=await fetch(`${baseUrl}${path}`,{signal:controller.signal,headers:{"X-Internal-Service-Key":process.env.INTERNAL_SERVICE_KEY||""}});if(!response.ok)throw new Error(`HTTP ${response.status}`);return response.json();}catch(error){console.error("Analytics dependency request failed",{dependency:name,error:error.message});throw new AppError(`${name} analytics temporarily unavailable`,503);}finally{clearTimeout(timeout);}}
