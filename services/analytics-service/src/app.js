import cors from "cors";import express from "express";import helmet from "helmet";import adminRoutes from "./routes/admin.routes.js";import {errorHandler,notFoundHandler} from "./middleware/error.middleware.js";const app=express();app.use(helmet());app.use(cors());app.use(express.json({limit:"10kb"}));app.get("/health",(_request,response)=>response.json({service:"analytics-service",status:"healthy"}));app.use("/api/admin/analytics",adminRoutes);app.use(notFoundHandler);app.use(errorHandler);export default app;
