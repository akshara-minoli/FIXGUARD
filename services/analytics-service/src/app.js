import cors from "cors";import express from "express";import helmet from "helmet";import adminRoutes from "./routes/admin.routes.js";import {errorHandler,notFoundHandler} from "./middleware/error.middleware.js";import {installMetrics} from "./metrics.js";const app=express();app.use(helmet());app.use(cors({origin:process.env.FRONTEND_URL??"http://localhost:5173"}));app.use(express.json({limit:"10kb"}));installMetrics(app);app.get("/health",(_request,response)=>response.json({service:"analytics-service",status:"healthy"}));app.use("/api/admin/analytics",adminRoutes);app.use(notFoundHandler);app.use(errorHandler);export default app;
