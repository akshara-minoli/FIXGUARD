import "dotenv/config";import app from "./app.js";import prisma from "./config/prisma.js";import{startConsumer,closeConsumer}from"./messaging/consumer.js";const port=Number(process.env.PORT||4006);try{await prisma.$connect();console.log("analytics-service connected to PostgreSQL");startConsumer();const server=app.listen(port,()=>console.log(`Analytics service listening on port ${port}`));async function shutdown(){server.close(async()=>{await closeConsumer();await prisma.$disconnect();process.exit(0);});}process.on("SIGTERM",shutdown);process.on("SIGINT",shutdown);}catch(error){console.error("Unable to start analytics-service:",error.message);await prisma.$disconnect();process.exit(1);}
