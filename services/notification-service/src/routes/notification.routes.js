import {Router} from "express";import {count,detail,list,read,readAll} from "../controllers/notification.controller.js";import {authenticate} from "../middleware/auth.middleware.js";import {asyncHandler} from "../utils/async-handler.js";const router=Router();router.use(authenticate);router.get("/",asyncHandler(list));router.get("/unread-count",asyncHandler(count));router.patch("/read-all",asyncHandler(readAll));router.get("/:id",asyncHandler(detail));router.patch("/:id/read",asyncHandler(read));export default router;
