import test from"node:test";import assert from"node:assert/strict";import app from"../src/app.js";import{processEvent}from"../src/messaging/event-processor.js";test("health and protected endpoints",async()=>{process.env.INTERNAL_SERVICE_KEY="test-internal-key-value";const s=app.listen(0);try{const base=`http://127.0.0.1:${s.address().port}`;assert.equal((await fetch(base+"/health")).status,200);assert.equal((await fetch(base+"/api/notifications")).status,401);assert.equal((await fetch(base+"/api/internal/notifications",{method:"POST"})).status,401);await assert.rejects(()=>processEvent({eventId:"00000000-0000-4000-8000-000000000000",eventType:"report.created",eventVersion:2,occurredAt:new Date().toISOString(),source:"report-service",data:{}}))}finally{s.close()}});
