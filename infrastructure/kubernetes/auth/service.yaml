apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: fixguard
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: fixguard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-service
  ports:
    - name: http
      port: 4001
      targetPort: http
      protocol: TCP
