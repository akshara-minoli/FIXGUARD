apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: fixguard
  labels:
    app.kubernetes.io/name: analytics-service
    app.kubernetes.io/part-of: fixguard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: analytics-service
  ports:
    - name: http
      port: 4006
      targetPort: http
      protocol: TCP
