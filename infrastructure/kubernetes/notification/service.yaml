apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: fixguard
  labels:
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/part-of: fixguard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: notification-service
  ports:
    - name: http
      port: 4005
      targetPort: http
      protocol: TCP
