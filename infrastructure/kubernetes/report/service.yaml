apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: fixguard
  labels:
    app.kubernetes.io/name: report-service
    app.kubernetes.io/part-of: fixguard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: report-service
  ports:
    - name: http
      port: 4002
      targetPort: http
      protocol: TCP
